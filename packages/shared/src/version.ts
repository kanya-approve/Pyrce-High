/**
 * Wire protocol version negotiated between client and server on first message.
 * Bump on any breaking change to opcode payloads or match-state shape.
 */
export const WIRE_PROTOCOL_VERSION = '0.1.0' as const;
